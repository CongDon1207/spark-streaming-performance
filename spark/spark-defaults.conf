# spark-defaults.conf
# Cấu hình mặc định cho Spark
#
# Các cấu hình quan trọng:
# - spark.sql.shuffle.partitions (mặc định cho shuffle operations)
# - Log level configuration
# - Memory settings
# - Serialization settings
# - Dynamic allocation settings